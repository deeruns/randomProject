import Login from "../../../../src/components/authentication/login/Login";

describe("Login Component", () => {
    it("starts with the sign-in button disables", () => {

    });
});

const render = (originalUrl: string) => {
    
}